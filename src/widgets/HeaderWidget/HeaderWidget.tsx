import Header from '@/components/molecules/Header';

export const HeaderWidget = () => {
  const headerText = 'React steam game app';

  return <Header>{headerText}</Header>;
};

export default HeaderWidget;
